چون برای هر Navigation property هنگام دسترسی یک Query جدید اجرا می‌شود.

مثال:
```csharp

var posts = context.Posts.ToList();

foreach(var p in posts)
{
    var c = p.Comments; // Lazy load → کوئری جدا
}

```
