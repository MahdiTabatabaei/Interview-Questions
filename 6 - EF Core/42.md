ویژگی **Cascade Delete** یعنی وقتی Parent حذف شود → Childها به صورت خودکار حذف می‌شوند.

تنظیم:
```csharp

modelBuilder.Entity<Order>()
    .HasOne(o => o.Customer)
    .WithMany(c => c.Orders)
    .OnDelete(DeleteBehavior.Cascade);

```
