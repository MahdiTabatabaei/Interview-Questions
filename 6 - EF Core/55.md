راه‌حل‌ها:

1. اضافه‌کردن FK Property
2. استفاده از Fluent API:
```csharp

.HasOne()
.WithMany()
.HasForeignKey()

```
3. اضافه‌کردن Navigation Property
4. اصلاح نوع داده FK و PK

(مثلاً int با Guid اشتباه نشود)