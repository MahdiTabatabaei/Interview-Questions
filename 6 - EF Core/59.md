راهکارها:

- استفاده از `AsNoTracking()`
- استفاده از `SplitQuery`
- کاهش تعداد `Include`
- Projection به DTO:
```csharp

.Select(x => new UserDto { Name = x.Name, OrdersCount = x.Orders.Count })

```
- استفاده از `CompiledQuery`
- و Indexگذاری مناسب در دیتابیس
