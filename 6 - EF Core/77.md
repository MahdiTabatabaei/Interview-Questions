## Attach در Many-to-Many

اگر Entity Attach شود ممکن است EF تصور کند Join تغییر کرده.

راه حل:
```csharp

context.Attach(student);
context.Entry(student).State = EntityState.Unchanged;

```

یا
```csharp

context.ChangeTracker.Clear();

```
