زمانی که فضای حافظه Stack تمام شود. رایج‌ترین علت آن **Recursion (بازگشت) بی‌پایان** است که باعث می‌شود متدها مدام روی استک چیده شوند و فضا پر شود.

**مثال:**
```csharp

void RecursiveMethod() {
    RecursiveMethod(); // خطا: StackOverflowException
}

```
