Language Integrated Query

در واقع LINQ ابزار **Query زدن روی داده‌ها در C#** است.
می‌تواند روی:

- collections
- database
- xml

مثال LINQ
```csharp

var result = users.Where(x => x.Age > 18)
.OrderBy(x => x.Name)
.ToList();

```

