استفاده از علامت `=>` (Lambda operator) برای تعریف متدها، Propertyها و Constructorهایی که فقط یک خط بدنه دارند.

**مثال:**
```csharp

public class User {
    public string Name { get; set; }
    // Expression-bodied property
    public string Greet => $"Hello {Name}"; 
}

```
