مثل `lock` است، اما می‌تواند **بین چند Process** هم کار کند، نه فقط Threadهای یک برنامه.

### مثال:
```csharp

using var mutex = new Mutex(true, "MyAppLock");
if (!mutex.WaitOne(5000))
{
    Console.WriteLine("App already running!");
}

```
