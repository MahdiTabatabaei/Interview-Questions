چون:

- هر event **یک reference قوی** به subscriber نگه می‌دارد.
- اگر unsubscribe نشود:
- در نتیجه object هرگز از حافظه جمع نمی‌شود.

## مثال:
```csharp

publisher.SomeEvent += subscriber.Handler; // حافظه نگه داشته می‌شود

// اگر:
publisher.SomeEvent -= subscriber.Handler; // لازم است

```
