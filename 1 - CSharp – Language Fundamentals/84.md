روشی برای چک کردن نوع داده و استخراج مقدار از آن در یک خط کد. کد را بسیار تمیزتر از `if/else`های طولانی می‌کند.

**مثال:**
```csharp

object data = "Hello";
if (data is string s && s.Length > 0)
    Console.WriteLine($"String of length {s.Length}");

```
