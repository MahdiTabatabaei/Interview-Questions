اگر enum برای انتخاب رفتار استفاده شود:
```csharp

public Payment(PaymentType type)
{
   if(type == PaymentType.Paypal) {}
   if(type == PaymentType.Card) {}
}

```
برای افزودن نوع جدید باید **کلاس تغییر کند** → نقض OCP.

راه حل:

**Strategy Pattern**
