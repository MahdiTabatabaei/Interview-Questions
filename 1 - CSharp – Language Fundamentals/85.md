نسخه خلاصه‌تر و قدرتمندتر `switch` سنتی (معرفی شده در C# 8). به صورت یک عبارت (Expression) نتیجه را برمی‌گرداند.

**مثال:**
```csharp

int day = 2;
string result = day switch {
    1 => "Saturday",
    2 => "Sunday",
    _ => "Other" // Default
};

```
