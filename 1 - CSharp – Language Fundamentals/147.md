### ReadAsync

منتظر می‌ماند تا داده برسد.
```csharp

var item = await channel.Reader.ReadAsync();

```
---

### TryRead

فوری اجرا می‌شود.
```csharp

if(channel.Reader.TryRead(out int value))
{
    Console.WriteLine(value);
}

```
