ابزار Channel بهتر است وقتی:

- عملیات **async** داریم
- ممکن است [[back‑pressure]] داشته باشیم
- حالت [[Producer‑Consumer Pipeline]] 
- نیاز به [[High Throughput]] 

---

### مثال pipeline
```csharp

var channel = Channel.CreateUnbounded<string>();

Task.Run(async () =>
{
    await channel.Writer.WriteAsync("job1");
});

await foreach (var job in channel.Reader.ReadAllAsync())
{
    Console.WriteLine(job);
}

```


---

# خلاصه بسیار ساده
![[32_Backpressure_HighThroughput_ProducerConsumerPipeline.png]]
