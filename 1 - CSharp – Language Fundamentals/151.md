```csharp

var channel = Channel.CreateUnbounded<int>();

async Task Consumer(string name)
{
    await foreach (var item in channel.Reader.ReadAllAsync())
    {
        Console.WriteLine($"{name} got {item}");
    }
}

Task.Run(() => Consumer("C1"));
Task.Run(() => Consumer("C2"));

for (int i = 0; i < 10; i++)
{
    await channel.Writer.WriteAsync(i);
}

channel.Writer.Complete();

```
هر message فقط به **یک consumer** می‌رسد.
