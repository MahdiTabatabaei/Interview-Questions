شبیه به **Lock** است اما اجازه می‌دهد **تعداد مشخصی از Threadها** هم‌زمان وارد شوند.

### مثال:
```csharp

SemaphoreSlim sem = new SemaphoreSlim(3); // حداکثر 3 Thread

await sem.WaitAsync(); // ورود کنترل‌شده
Console.WriteLine("Thread entered");
sem.Release(); // خروج

```
