در پیاده‌سازی داخلی:

- atomic operations
- lock-free structures
- synchronization primitives

استفاده می‌کند.

بنابراین:

چند producer و consumer می‌توانند همزمان کار کنند.

بدون نیاز به:
```markdown

lock
Monitor
Mutex

```
