- در این حالت Reader دیگر داده جدید دریافت نمی‌کند
- حلقه `ReadAllAsync` پایان می‌یابد

---

### مثال
```csharp

await foreach (var item in channel.Reader.ReadAllAsync())
{
    Console.WriteLine(item);
}

Console.WriteLine("Channel finished");

