برای **اختصاص حافظه در stack به صورت مستقیم** استفاده می‌شود.

معمولاً با `Span` استفاده می‌شود.
```csharp

Span<int> numbers = stackalloc int[5];

numbers[0] = 10;
numbers[1] = 20;

```

---

✅ مزایا

- بدون GC
- سریع‌تر
- مناسب عملیات کوتاه

---

✅ محدودیت

- فقط در stack
- اندازه محدود