وقتی **object هنوز reference دارد ولی دیگر استفاده نمی‌شود**.

مثال:
```csharp

static List<object> cache = new List<object>();

void Add()
{
    cache.Add(new object());
}

```
چون reference باقی مانده → GC آن را حذف نمی‌کند.
