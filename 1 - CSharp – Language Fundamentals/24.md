✅ **string**

- متن کوتاه
- عملیات کم
```csharp

string name = "Ali";

```
---

✅ **StringBuilder**

- حلقه‌ها
- عملیات concat زیاد
- فایل یا text بزرگ
```csharp

StringBuilder sb = new StringBuilder();

```
✅ قانون ساده

اگر **> 5 یا 10 تغییر رشته دارید → StringBuilder**

✅ از `string` استفاده کن اگر:

- مقدار کم
- تغییرات کم
- خوانایی مهم‌تر از performance

✅ از `StringBuilder` استفاده کن اگر:

- اگر loop داری
- متن بزرگ
- اگر concat زیاد

> هر  Value Type مقدار را نگه می‌دارد و هنگام assignment کپی می‌شود، اما Reference Type فقط آدرس حافظه را نگه می‌دارد.
> 
 و string یک Reference Type است ولی [[196 | Immutable]] می‌باشد، یعنی با هر تغییر یک object جدید ساخته می‌شود.
> 
> برای عملیات concat زیاد، بهتر است از StringBuilder استفاده کنیم چون mutable است و performance بهتری دارد.