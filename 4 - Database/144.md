TTL یعنی:

**Time To Live**

مدت زمانی که یک key در Redis باقی می‌ماند.

بعد از آن **خودکار حذف می‌شود.**

مثال:
```markdown

SET token123 "abc" EX 60

```
یعنی:

این token بعد از **60 ثانیه حذف می‌شود.**

مثال واقعی:

OTP (One‑Time Password)

کد پیامکی:

- بعد از 2 دقیقه منقضی می‌شود.
