برای **وارد کردن حجم بسیار زیاد داده** به SQL Server با سرعت بالا استفاده می‌شود.

مثال:
```sql

BULK INSERT dbo.Customers
FROM 'C:\data.csv'
WITH (FIELDTERMINATOR = ',', ROWTERMINATOR = '\n');

```
مثال واقعی:

مثل خالی‌کردن یک کامیون بار با لیفتراک، نه با دست.
