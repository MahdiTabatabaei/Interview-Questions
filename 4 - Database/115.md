وقتی شرط WHERE از نوع بازه باشد:
```sql

WHERE OrderDate BETWEEN '2024-01-01' AND '2024-02-01'

```
در SQL Server شروع به خواندن از نقطه شروع می‌کند و تا نقطه پایان ادامه می‌دهد.

مثال زندگی:

مثل ورق‌زدن کتاب از صفحه ۲۰ تا ۴۰.