وقتی یک تراکنش **داده‌ تغییر یافته و Commit نشده** توسط تراکنش دیگری را می‌خواند.

مثال

تراکنش A موجودی حساب را 100 واحد کم می‌کند (هنوز Commit نشده)

تراکنش B موجودی را می‌خواند و مقدار کمتر را می‌بیند.

اگر تراکنش A Rollback شود، تراکنش B داده‌ای را خوانده که **اصلاً وجود نداشت** → Dirty Read.