- ساخت Index مناسب روی ستون بازه
- تبدیل توابع غیر قابل SARGABLE به قابل جستجو
- ایجاد Composite Index با ستون‌های مرتب

مثال:

بد:
```sql

WHERE YEAR(OrderDate) = 2024

```
خوب:
```sql

WHERE OrderDate >= '2024-01-01'
  AND OrderDate < '2025-01-01'

```
