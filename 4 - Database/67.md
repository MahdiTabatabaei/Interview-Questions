چون `SQL` نمی‌تواند از ابتدای رشته جستجو کند.

برای Index لازم است ابتدا مشخص باشد.

- در این حالت ‘%abc%’ → نمی‌توان از Index استفاده کرد
- در این حالت ‘abc%’ → از Index استفاده می‌شود

مثال

مثل اینکه بگویی:

“کتابی می‌خوام که وسطش کلمه عشق داره!”

کتابداری مجبور است تمام صفحات همه کتاب‌ها را بخواند.