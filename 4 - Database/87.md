مثال:
```sql

SELECT * 
FROM Orders 
WHERE CustomerID = @CustomerID 
OPTION (RECOMPILE);

```
⇒ SQL Server مجبور می‌شود Plan جدید برای هر اجرا بسازد.

یا:
```sql

OPTION (OPTIMIZE FOR UNKNOWN)

```
⇒ به جای استفاده از مقدار فعلی، از **میانگین توزیع داده‌ها** Plan می‌سازد.