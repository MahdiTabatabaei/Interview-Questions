هر دو برای اجرای **background tasks** استفاده می‌شوند.

---

## IHostedService

یک interface است.
```csharp

public class MyService : IHostedService
{
    public Task StartAsync(CancellationToken token)
    {
        return Task.CompletedTask;
    }

    public Task StopAsync(CancellationToken token)
    {
        return Task.CompletedTask;
    }
}

```
باید همه چیز را خودتان مدیریت کنید.

---

## BackgroundService

یک کلاس abstract است که از IHostedService ارث‌بری کرده.

کار را ساده‌تر می‌کند.
```csharp

public class Worker : BackgroundService
{
    protected override async Task ExecuteAsync(CancellationToken stoppingToken)
    {
        while(!stoppingToken.IsCancellationRequested)
        {
            Console.WriteLine("running...");
            await Task.Delay(5000);
        }
    }
}

```
---

### تفاوت
![[3_difference_IHostedService_vs_BackgroundService.png]]
