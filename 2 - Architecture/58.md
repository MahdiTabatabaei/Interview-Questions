قفل مشترک بین چند سرویس برای جلوگیری از race condition.

ابزارها:

- Redis RedLock
- Zookeeper
- Consul

### مثال C# با Redis
```csharp

var redLock = await redisLockFactory.CreateLockAsync("order:123", TimeSpan.FromSeconds(30));

```
