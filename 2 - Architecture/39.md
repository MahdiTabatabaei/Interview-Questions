به‌جای Outbox از **لگ دیتابیس** استفاده می‌کنیم:

- تغییرات دیتابیس در WAL log (مثل MySQL Binlog یا Postgre WAL) ثبت می‌شود
- سیستم این لگ را خوانده و Event تولید می‌کند

ابزار:

Debezium

این روش تغییرات DB را **Stream** می‌کند.
