وقتی ساختار Event تغییر کند مشکل ایجاد می‌شود.

روش‌ها:

### 1) Versioning
```markdown

OrderCreatedV1
OrderCreatedV2

```
### 2) Backward compatible schema

فیلد جدید optional باشد.

### 3) Schema Registry

در Kafka از Confluent Schema Registry استفاده می‌کند.
