### Read Through

وقتی cache miss شود، cache خودش از DB می‌خواند.
```markdown

app → cache → db

```
### Write Through

داده همزمان در cache و DB نوشته می‌شود.

### مثال
```markdown

Write → Cache → Database

```
