رخدادی است در دامنه که **اتفاق مهمی را بیان می‌کند** و مدل باید به آن واکنش دهد.

مثال:

OrderPlaced

PaymentConfirmed

مثال C#:
```csharp

public record OrderPlaced(Guid OrderId) : IDomainEvent;

```
