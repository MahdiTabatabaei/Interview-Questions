ابزار **MediatR** در اصل پیاده‌سازی الگوی **Mediator Pattern** در .NET است. مشکل اصلی‌ای که حل می‌کند **کاهش وابستگی مستقیم (tight coupling)** بین اجزای برنامه است.

## مشکل قبل از MediatR

فرض کنید یک Controller چند سرویس مختلف را صدا می‌زند:
```csharp

public class OrdersController : Controller
{
    private readonly IOrderService _orderService;
    private readonly IEmailService _emailService;
    private readonly IInventoryService _inventoryService;

    public OrdersController(
        IOrderService orderService,
        IEmailService emailService,
        IInventoryService inventoryService)
    {
        _orderService = orderService;
        _emailService = emailService;
        _inventoryService = inventoryService;
    }

    public async Task<IActionResult> Create(CreateOrderDto dto)
    {
        var order = await _orderService.Create(dto);
        await _inventoryService.Reserve(order);
        await _emailService.SendConfirmation(order);

        return Ok();
    }
}

```
مشکلات:

- Controller وابسته به چندین سرویس است
- تغییر در business logic باعث تغییر در controller می‌شود
- تست سخت‌تر می‌شود
- مسئولیت‌ها مخلوط می‌شوند
- وابستگی‌ها زیاد می‌شوند

---

# راه‌حل MediatR

به جای اینکه Controller مستقیماً سرویس‌ها را صدا بزند، فقط یک **Request** ارسال می‌کند.
```csharp

public class CreateOrderCommand : IRequest<Order>
{
    public string ProductId { get; set; }
}

```
Handler:
```csharp

public class CreateOrderHandler : IRequestHandler<CreateOrderCommand, Order>
{
    private readonly IOrderService _orderService;

    public CreateOrderHandler(IOrderService orderService)
    {
        _orderService = orderService;
    }

    public async Task<Order> Handle(CreateOrderCommand request, CancellationToken ct)
    {
        return await _orderService.Create(request);
    }
}

```
Controller:
```csharp

public class OrdersController : Controller
{
    private readonly IMediator _mediator;

    public OrdersController(IMediator mediator)
    {
        _mediator = mediator;
    }

    public async Task<IActionResult> Create(CreateOrderCommand command)
    {
        var result = await _mediator.Send(command);
        return Ok(result);
    }
}

```
الان Controller فقط با **Mediator** کار می‌کند.

---

# مشکلاتی که MediatR حل می‌کند

### 1️⃣ کاهش Coupling

به جای اینکه:
```markdown

Controller → Service → Service → Service

```
می‌شود:
```markdown

Controller → Mediator → Handler

```
---

### 2️⃣ جداسازی Command و Query (CQRS)

معمولاً با MediatR این ساختار ساخته می‌شود:
```markdown

Commands
Queries
Handlers

```
---

### 3️⃣ مدیریت Cross‑Cutting Concerns

با **Pipeline Behaviors** می‌توان موارد زیر را جدا کرد:

- Logging
- Validation
- Caching
- Transaction
- Authorization
مثال:
```markdown

Request
 ↓
ValidationBehavior
 ↓
LoggingBehavior
 ↓
Handler

```

### 4️⃣ تست‌پذیری بهتر

هر Handler مستقل تست می‌شود.

---

### 5️⃣ کوچک‌تر شدن Controller

و Controller تبدیل می‌شود به:
```markdown

thin controller
fat handler

```

---
# جمع‌بندی کوتاه مصاحبه‌ای

اگر در مصاحبه بپرسند:

ابزار **MediatR چه مشکلی را حل می‌کند؟**

پاسخ خوب:

> ابزار MediatR پیاده‌سازی الگوی Mediator است که برای کاهش coupling بین اجزای سیستم استفاده می‌شود. به جای اینکه Controller مستقیماً به چندین Service وابسته باشد، درخواست‌ها به Mediator ارسال می‌شوند و Handler مناسب آن را پردازش می‌کند. این کار باعث جداسازی مسئولیت‌ها، تست‌پذیری بهتر و پیاده‌سازی ساده‌تر CQRS و Cross‑Cutting concerns می‌شود.