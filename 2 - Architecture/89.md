الگوی رایج برای cache.

مراحل:

1. برنامه cache را چک می‌کند
2. اگر نبود → DB
3. نتیجه در cache ذخیره می‌شود

### مثال #C#
```csharp

var user = await cache.GetAsync(id);

if(user == null)
{
    user = await db.Users.FindAsync(id);
    await cache.SetAsync(id, user);
}

```
