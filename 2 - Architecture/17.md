الگوی Repository واسطی است برای کار با Aggregate‌ها بدون وابستگی به دیتابیس.

اما [[anti‑pattern]] زمانی می‌شود که:

- فقط پاس‌دهنده بین ORM و Domain باشد
- مدل‌های لاجیک را پنهان کند
- استفاده از IQueryable بیرون بدهد (نقض encapsulation)