گاهی read model هنوز آپدیت نشده است.

راهکارها:

### 1) Eventual consistency

کلاینت چند لحظه صبر می‌کند.

### 2) Read-your-own-writes

کاربر داده خودش را مستقیم از write model می‌خواند.

### 3) UI refresh

سیستم اعلام می‌کند:
```markdown

Processing...

```
