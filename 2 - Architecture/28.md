همه سرویس‌ها Log را به یک مقصد مشترک ارسال می‌کنند:

مراحل:

- ارسال Log به Log Collector
- ذخیره در Storage
- مشاهده از طریق UI

ابزار و Stack رایج:

- ELK (Elasticsearch + Logstash + Kibana)
- Loki + Promtail + Grafana
- Splunk
- Datadog

---

 
یک نمونه از لاگ مرکزی با ترکیب این سه مفهومه:

1. **Access Logging (ثبت همه request ها)**
2. **Structured Logging (لاگ ساخت‌یافته قابل سرچ در Elastic)**
3. **Correlation + Observability (قابل دنبال‌کردن بین سرویس‌ها)**

 بهترین طراحی Production‑Grade رو برای معماری با این مشخصات:

- ✅ Kubernetes  
- ✅ ASP.NET Core Microservices  
- ✅ Traefik  
- ✅ Keycloak  
- ✅ Elasticsearch  

---

# ✅ اول یک اصل مهم

اگر هدف این باشد که :

> ببینم چه درخواستی، با چه پارامترهایی، به کدوم API خورده

باید تصمیم گرفته شود :

🔴 آیا نیاز است **بدنه درخواست (Request Body)** هم لاگ شود؟  
⚠ این کار از نظر امنیت و Performance حساس است.

✅ بهترین Practice:

- QueryString ✅
- Route ✅
- Method ✅
- StatusCode ✅
- Duration ✅
- CorrelationId ✅
- UserId ✅
- IP ✅
- Body ❌ فقط اگر ضروری و با Masking

---

# ✅ بهترین معماری پیشنهادی (Enterprise Pattern)

## مرحله 1️⃣ — همه requestها در ASP.NET Core لاگ شوند

نباید فقط به Traefik تکیه کرد.

چرا؟

چون Traefik:

- بدنه درخواست را نمی‌بیند
- و context اپلیکیشن (UserId, Business Data) را ندارد
- فقط لایه HTTP را می‌بیند

پس بهترین روش:

> یک Middleware مرکزی در تمام میکروسرویس‌ها برای Logging درخواست‌ها

---

# ✅ مرحله 2️⃣ — پیاده‌سازی Middleware استاندارد

### ✅ Middleware کامل برای Request Logging

```csharp
public class RequestLoggingMiddleware
{
    private readonly RequestDelegate _next;
    private readonly ILogger<RequestLoggingMiddleware> _logger;

    public RequestLoggingMiddleware(
        RequestDelegate next,
        ILogger<RequestLoggingMiddleware> logger)
    {
        _next = next;
        _logger = logger;
    }

    public async Task Invoke(HttpContext context)
    {
        var stopwatch = Stopwatch.StartNew();

        var request = context.Request;

        var correlationId = context.TraceIdentifier;

        var userId = context.User?.Identity?.IsAuthenticated == true
            ? context.User.FindFirst("sub")?.Value
            : null;

        string queryString = request.QueryString.HasValue
            ? request.QueryString.Value
            : null;

        await _next(context);

        stopwatch.Stop();

        _logger.LogInformation("HTTP Request completed {@RequestLog}",
            new
            {
                CorrelationId = correlationId,
                Method = request.Method,
                Path = request.Path,
                QueryString = queryString,
                StatusCode = context.Response.StatusCode,
                DurationMs = stopwatch.ElapsedMilliseconds,
                UserId = userId,
                IP = context.Connection.RemoteIpAddress?.ToString(),
                Service = "order-service"
            });
    }
}
```

در Program.cs:

```csharp
app.UseMiddleware<RequestLoggingMiddleware>();
```

---

# ✅ مرحله 3️⃣ — استفاده از Serilog با خروجی JSON

چرا Serilog؟

چون:

- Structured logging حرفه‌ای دارد
- Integration عالی با Elastic دارد
- Enrichment قوی دارد

### نصب:

```
Serilog.AspNetCore
Serilog.Sinks.Console
Serilog.Formatting.Compact
```

### appsettings.json

```json
"Serilog": {
  "Using": [ "Serilog.Sinks.Console" ],
  "MinimumLevel": "Information",
  "WriteTo": [
    {
      "Name": "Console",
      "Args": {
        "formatter": "Serilog.Formatting.Compact.RenderedCompactJsonFormatter, Serilog.Formatting.Compact"
      }
    }
  ],
  "Enrich": [ "FromLogContext", "WithMachineName" ]
}
```

✅ خروجی به stdout  
✅ نوع JSON  
✅ آماده جمع‌آوری توسط Fluent Bit

---

# ✅ مرحله 4️⃣ — جمع‌آوری لاگ‌ها در Kubernetes

روی کلاستر باید نصب کرد:

### Fluent Bit (به صورت DaemonSet)

وظیفه:

```
/var/log/containers/*.log
     ↓
Parse JSON
     ↓
Add K8s metadata
     ↓
Send to Elasticsearch
```

---

# ✅ مرحله 5️⃣ — ایندکس استاندارد در Elasticsearch

پیشنهاد خوب:

ایجاد Index Pattern جدا برای access log ها:

```
api-access-logs-YYYY.MM.DD
```

نمونه Mapping پیشنهادی:

| Field | Type |
|-------|------|
| correlationId | keyword |
| method | keyword |
| path | keyword |
| queryString | text |
| statusCode | integer |
| durationMs | long |
| userId | keyword |
| service | keyword |
| ip | ip |
| timestamp | date |

---

# ✅ مرحله 6️⃣ — نقش Traefik

می توان Traefik Access Log را هم فعال کرد:

```yaml
accessLog:
  format: json
```

این قابلیت را می‌دهد:

- total latency
- upstream latency
- client IP
- response size

خیلی عالی برای:

- بررسی 5xx
- بررسی حملات
- بررسی traffic

اما باز هم Business context ندارد.

---

# ✅ مرحله 7️⃣ — Correlation بین سرویس‌ها (خیلی مهم)

اگر سرویس A → سرویس B را صدا بزند:

باید CorrelationId حفظ شود.

در HttpClient:

```csharp
httpClient.DefaultRequestHeaders.Add(
    "X-Correlation-ID",
    correlationId);
```

در Middleware:

```csharp
var correlationId = context.Request.Headers["X-Correlation-ID"]
    .FirstOrDefault() ?? Guid.NewGuid().ToString();
```

✅ حالا می‌توان یک request کامل را در تمام سرویس‌ها دنبال کرد.

---

# ✅ مرحله 8️⃣ — اگر نیاز باشد Body را هم لاگ کرد

⚠️ خطرناک از نظر Performance

اگر لازم است:

```csharp
context.Request.EnableBuffering();

using var reader = new StreamReader(
    context.Request.Body,
    Encoding.UTF8,
    leaveOpen: true);

var body = await reader.ReadToEndAsync();
context.Request.Body.Position = 0;
```

اما:

✅ فقط برای Debug environment  
✅ یا با Mask کردن فیلدهای حساس  
✅ هرگز Password و Token را لاگ نکن

---

# ✅ نتیجه نهایی معماری حرفه‌ای

```
Client
   ↓
Traefik (Access log JSON)
   ↓
Microservice (Serilog + Middleware)
   ↓
stdout
   ↓
Fluent Bit (DaemonSet)
   ↓
Elasticsearch
   ↓
Kibana
```

---

# ✅ در Kibana چه چیزهایی می‌توان دید؟

مثال Query:

🔎 همه درخواست‌های `/api/orders`

```
path : "/api/orders"
```

🔎 همه Error ها در order-service

```
service : "order-service" AND statusCode >= 500
```

🔎 یک درخواست خاص

```
correlationId : "abc-123"
```

🔎 درخواست‌های یک کاربر

```
userId : "54a1f"
```

---

# ✅ بهترین طراحی نهایی (Enterprise Level)

اگر بخواهیم حرفه‌ای‌ترین پیشنهاد را بدهم:

✅ Serilog  
✅ CorrelationId  
✅ Fluent Bit  
✅ Elasticsearch  
✅ Kibana  
✅ Traefik Access Logs  
✅ Alerting روی 5xx spike  
✅ ILM policy برای حذف لاگ‌های قدیمی  

---

# ⚠️ نکات مهم امنیتی

- لاگ کردن JWT ممنوع
- لاگ کردن Password ممنوع
- PII را Mask کن
- Retention policy داشته باش
- Index rollover فعال کن

