پیامی که **هر بار خوانده شود → باعث خطا می‌شود**.

مثلاً:

- داده خراب
- فرمت اشتباه
- مقدار NULL غیرمجاز

این پیام همیشه Fail می‌شود، حتی با Retry.

راه‌حل:

- انتقال به **DLQ**
- بررسی دستی
- تغییر وضعیت به Fix کردن پیام یا حذف آن
