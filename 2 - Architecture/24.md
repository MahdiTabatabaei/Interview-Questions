در Microservices آدرس سرویس‌ها ثابت نیست.

مفهوم Service Discovery یعنی:

- هر سرویس خودش را ثبت کند (Register)
- سرویس‌ها بتوانند همدیگر را پیدا کنند (Lookup)

این کار معمولاً با Service Registry انجام می‌شود.