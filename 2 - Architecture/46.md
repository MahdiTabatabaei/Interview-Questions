پیام‌هایی که **چند بار Fail می‌شوند** به یک صف مخصوص منتقل می‌شوند.

موارد Fail:

- deserialization error
- validation error
- business rule violation

DLQ جلوگیری می‌کند از:

- block شدن Queue اصلی
- retry بی‌نهایت

### مثال C# (MassTransit)
```csharp

cfg.ReceiveEndpoint("orders", e =>
{
    e.BindDeadLetterQueue();
});

```
