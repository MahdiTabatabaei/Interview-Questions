مفهوم XSS یعنی تزریق اسکریپت در صفحه.

راه‌حل‌ها:

- encode خروجی
- validate input
- CSP headers

مثال:
```csharp

@Html.Encode(Model.Name)

```
یا در Razor به صورت پیش‌فرض:
```csharp

@Model.Name

```
