برای سرویس‌هایی که کاربر ندارند (server‑to‑server communication).

کلاینت با **ClientId و Secret** احراز هویت می‌شود.

🧩 مثال C#:
```csharp

var tokenResponse = await client.RequestClientCredentialsTokenAsync("api1");

```
📘 مثال واقعی:

دو شرکت با کلید‌های مخصوص‌شان داده بین سرورها رد و بدل می‌کنند، بدون دخالت انسان.
