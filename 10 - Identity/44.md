تعریف Scope یعنی **سطح دسترسی که کلاینت درخواست می‌کند**.

مثال:
```markdown

orders.read
orders.write
profile
email

```
مثال واقعی:

اپلیکیشن می‌گوید:

> من فقط اجازه خواندن اطلاعات سفارش‌ها را می‌خواهم.

مثال C#:
```csharp

new ApiScope("orders.read", "Read Orders")

```
