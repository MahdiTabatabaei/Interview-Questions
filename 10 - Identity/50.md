وقتی کاربر Logout کند:

1. ابتدا Session در IdentityServer پاک می‌شود
2. همه کلاینت‌ها logout می‌شوند

در ASP.NET:
```csharp

await HttpContext.SignOutAsync();

```
و IdentityServer سپس logout را به سایر client ها اطلاع می‌دهد.
