تعریف **OpenID Connect (OIDC)** یعنی **افزودن احراز هویت (Identity)** بر پایه‌ی OAuth 2.0.

| OAuth 2.0 | صرفاً مجوز دسترسی |

| OpenID Connect | احراز هویت + اطلاعات کاربر |

📘 مثال واقعی:

OAuth فقط می‌گوید “اجازه داری وارد شوی”.

OIDC می‌گوید “تو واقعاً علی هستی!”
