سروری در OIDC که کلاینت می‌تواند مشخصات کاربر را با Access Token دریافت کند.

📘 مثال C#:
```csharp

var response = await client.GetUserInfoAsync(accessToken);

```
📘 مثال واقعی:

مثل تماس با دفتر ثبت احوال برای دریافت جزئیات هویت فرد با شماره ملی.
