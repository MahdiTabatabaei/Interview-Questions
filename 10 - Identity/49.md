مفهوم SSO یعنی:

کاربر **یکبار لاگین کند و در چند اپ وارد شود**.

در این حالت IdentityServer یک **cookie session مرکزی** ایجاد می‌کند.

مثال:
```markdown

App1 → Login
App2 → already logged in

```
مثال واقعی:

ورود به Gmail باعث ورود به YouTube هم می‌شود.
