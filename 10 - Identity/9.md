1. **ApplicationUser** — مدل کاربر (User Class)
2. **UserManager** — مدیریت کاربران
3. **SignInManager** — مدیریت ورود و خروج
4. **RoleManager** — مدیریت نقش‌ها
5. **DbContext (IdentityDbContext)** — دسترسی به دیتابیس

📘 **ارتباط:**
```csharp

UserManager → ApplicationUser → RoleManager → IdentityDbContext

```
