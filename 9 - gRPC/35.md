با استفاده از:

- Trailers
- RpcException
- Custom Metadata
```csharp

var trailers = new Metadata { { "error-code", "USER_001" } };

throw new RpcException(new Status(StatusCode.InvalidArgument, "Error"), trailers);

```
