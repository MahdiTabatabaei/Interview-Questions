در Protobuf شماره فیلد **identifier اصلی** است.

اگر تغییر کند:

- پیام‌های قدیمی قابل خواندن نیستند
- serialization خراب می‌شود

مثال:
```markdown

string name = 1;

```
اگر تغییر دهید:
```markdown

string name = 5;

```
داده‌های قبلی corrupt می‌شوند.
