فایل `.proto` فایل تعریف قرارداد gRPC است.

شامل:

- message
- service
- rpc methods
- data types

مثال:
```csharp

syntax = "proto3";

service UserService {
 rpc GetUser(UserRequest) returns (UserResponse);
}

message UserRequest {
 int32 id = 1;
}

message UserResponse {
 string name = 1;
}

```
