دو مدل:

1. **L4 Load Balancer**

فقط TCP را توزیع می‌کند (NGINX/TCP LB)

2. **L7 Load Balancer**

HTTP/2 را می‌فهمد (Envoy بهترین گزینه)


در Kubernetes، gRPC معمولاً همراه با **Envoy** یا **Istio** استفاده می‌شود.

C# Client-side load balancing:
```csharp

var channel = GrpcChannel.ForAddress("dns:///myservice");

```
