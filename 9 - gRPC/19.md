Client چند request می‌فرستد

Server یک response می‌دهد.

proto:
```csharp

rpc UploadLogs(stream LogRequest) returns (UploadResult);

```
مثال C#:
```csharp

var call = client.UploadLogs();

await call.RequestStream.WriteAsync(new LogRequest());
await call.RequestStream.WriteAsync(new LogRequest());

await call.RequestStream.CompleteAsync();

var result = await call;

```
مثال واقعی:

آپلود chunk های فایل.
