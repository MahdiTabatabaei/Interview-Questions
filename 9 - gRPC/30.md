روش‌ها:

1. **Metadata header** (محبوب‌ترین)
2. TLS Client Certificates
3. OAuth2 / JWT
4. API Keys

C#:
```csharp

var headers = new Metadata { 
    { "api-key", "12345" }
};

var reply = await client.GetUserAsync(request, headers);

```
