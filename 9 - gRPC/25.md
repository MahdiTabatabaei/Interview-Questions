در HTTP/2:

- چند request می‌توانند روی **یک اتصال TCP** همزمان ارسال شوند.
- هر request «Stream ID» جدا دارد.
- بدون صف و بلاک شدن.

مثل بزرگراه 4 بانده که چند خودرو می‌توانند همزمان حرکت کنند.

این باعث می‌شود:

- latency کم
- throughput بیشتر
