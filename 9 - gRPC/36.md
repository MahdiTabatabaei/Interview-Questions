HTTP:

- فقط مربوط به وب
- text-based
- قابل استفاده در REST

gRPC:

- به RPC مربوط است
- مرتبط با رفتار برنامه (business logic)
- روی HTTP/2 سوار است اما مستقل از HTTP status

مثال:

HTTP 200 + gRPC Status = NOT_FOUND

سرویس از نظر HTTP موفق بوده

اما از نظر gRPC کاربر پیدا نشده.