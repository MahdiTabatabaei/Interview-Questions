Stub = کدی در Client که رفتار مشابه سرویس واقعی را شبیه‌سازی می‌کند.

Client به‌جای اینکه مستقیماً HTTP call بزند:
```csharp

client.GetUser(new UserRequest())

```
Stub خودش:

- serialization
- ارسال درخواست
- دریافت پاسخ

را انجام می‌دهد.

در C#:
```csharp

var client = new UserService.UserServiceClient(channel);

```
این **client** همان Stub است.
