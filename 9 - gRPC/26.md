دلایل:

- Binary data → حجم کم
- HTTP/2 → کاهش overhead
- Multiplexing → بدون انتظار
- Connection reuse → بدون handshake
- Serialization سریع Protobuf

مثال:

اگر JSON از 1 KB به 200 B برسد

زمان انتقال چند برابر کاهش می‌یابد.
