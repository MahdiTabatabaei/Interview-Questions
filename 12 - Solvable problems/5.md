پس سناریوی می‌شود.


```text
URL Snapshot / Content Cache Service
```

یعنی:

کاربر لینک می‌دهد →
سیستم محتوای صفحه را fetch می‌کند →
ذخیره می‌کند →
یک لینک کوتاه می‌دهد →
هرکس لینک کوتاه را باز کرد همان محتوا را ببیند.

یعنی شبیه:
- archive.today
- cached pages
- instant view
- article mirror


-----------------------------------

#### حالا دقیقاً چه چیزی ذخیره می‌کنیم؟


سه حالت داری:

حالت 1 — ذخیره HTML خام


یعنی:

```text
GET https://site.com/page
↓
کل HTML ذخیره شود
```

مزایا:
- سریع
- ساده
- سبک پردازشی

معایب:
- ممکن است JS وابسته باشد
- بعضی سایت‌ها درست render نشوند

-----------------------------------

حالت 2 — ذخیره Clean HTML



script/style حذف شود.

مثلاً:

```html
<script></script>
<style></style>
```

پاک شوند.

مزایا:
- حجم کمتر
- امن‌تر
- سریع‌تر

این بهترین گزینه برای اکثر پروژه‌هاست.

-----------------------------------

حالت 3 — ذخیره Rendered Page


یعنی:
- Playwright
- Puppeteer
- Headless Chrome

صفحه render شود بعد ذخیره شود.

برای سایت‌های SPA:
- React
- Vue
- Angular

لازم می‌شود.

ولی:
- CPU سنگین
- RAM سنگین
- Scale سخت‌تر

است.

-----------------------------------
پیشنهاد 


اول فقط:
- HTML خام
یا
- Clean HTML

ذخیره کن.

بعداً اگر لازم شد:
- Render Engine

اضافه کن.

-----------------------------------

### معماری 


الان Flow این است:

```text
Client
   |
   v
API
   |
   v
Queue
   |
   v
Worker
   |
   +--> Download HTML
   |
   +--> Clean HTML
   |
   +--> Save
   |
   v
DB/Object Storage
```

-----------------------------------

### چرا  Queue لازم داریم؟


چون fetch کردن URL کار سنگینی است.

مثلاً:

- سایت کند باشد
- باعث  timeout شود
- ۵۰ هزار درخواست بیاید

اگر مستقیم داخل API انجام شود :

- در این حالت API قفل می‌کند

پس Queue  لازم است.

-----------------------------------

### چرا Worker  لازم است؟


چون:

- دانلود صفحه IO سنگین است
- و parsing زمان می‌برد
- بعضی سایت‌ها کند هستند

و API نباید منتظر بماند.

-----------------------------------

### چرا Redis خوب است؟


چون:

اگر ۱۰۰۰ نفر یک URL یکسان بدهند:

```text
https://news.com/a
```

نباید:
- ۱۰۰۰ بار دانلود شود

پس Redis می‌گوید:

"قبلاً این URL کش شده"

-----------------------------------
معماری پیشنهادی واقعی
-----------------------------------

```text
                ┌────────────┐
                │ Client     │
                └─────┬──────┘
                      │
                      ▼
              ┌──────────────┐
              │ ASP.NET API  │
              └──────┬───────┘
                     │
                     ▼
              ┌──────────────┐
              │ Redis Cache  │
              └──────┬───────┘
                     │
         Not Found   ▼
              ┌──────────────┐
              │ RabbitMQ     │
              └──────┬───────┘
                     │
                     ▼
              ┌──────────────┐
              │ Worker       │
              └──────┬───────┘
                     │
                     ▼
              ┌──────────────┐
              │ Fetch HTML   │
              └──────┬───────┘
                     │
                     ▼
              ┌──────────────┐
              │ PostgreSQL   │
              └──────────────┘
```

-----------------------------------

### حالا چرا PostgreSQL؟



چون:
- HTML ممکن است بزرگ باشد
- query لازم داری
- index لازم داری
- transactional است

ولی…

-----------------------------------

### نکته مهم:

### چطور HTML را داخل DB ذخیره کنیم؟


برای Scale بالا:
نه.

بهتر است:

DB:
- فقط metadata

Object Storage:
- محتوا

-----------------------------------

### طراحی حرفه‌ای


DB:

```text
Id
ShortCode
Url
ContentPath
CreatedAt
```

مثلاً:

```text
ContentPath:
pages/ab/cd/aZ91x.html
```

خود فایل:
- MinIO
- S3
- Azure Blob

ذخیره شود.

-----------------------------------

### چرا فایل را جدا ذخیره کنیم؟


اگر HTML داخل DB باشد:
- DB خیلی بزرگ می‌شود
- backup سخت می‌شود
- read کند می‌شود

ولی Object Storage:

- ارزان
- سریع
- و scalable

است.

-----------------------------------

### بهترین طراحی Production



```text
PostgreSQL:
metadata only

S3/MinIO:
html files
```

-----------------------------------

### لینک کوتاه چطور ساخته شود؟


باز هم:

```text
Base62
```

بهترین است.

مثلاً:

```text
123456 => k9Xa2
```

-----------------------------------

### و API ها


1. Create
-----------------------------------

```http
POST /pages
{
   "url":"https://example.com"
}
```

Response:

```json
{
   "shortCode":"k9Xa2",
   "status":"processing"
}
```

-----------------------------------
2. Resolve
-----------------------------------

```http
GET /p/k9Xa2
```

Response:

```html
<html>...</html>
```

یا:

```json
{
   "status":"processing"
}
```

-----------------------------------

#### چرا HTML را Cache کنیم؟


چون:

اگر هر بار:
- از S3
- یا DB

بخوانی،
در فشار بالا کند می‌شود.

پس:

```text
Redis Cache
```

خیلی مهم است.

-----------------------------------

### جریان Flow واقعی Resolve



```text
GET /p/k9Xa2

↓
Redis

↓ found
return html

↓ not found

PostgreSQL

↓
S3

↓
Cache Redis

↓
Return
```

-----------------------------------

### چرا این طراحی Scale می‌شود؟


چون همه چیز جدا شده:

درواقع  API

فقط request می‌گیرد.

می‌توانی:

- ۲ تا
- ۲۰ تا
- ۲۰۰ تا

کنی.

-----------------------------------

###  بخش Worker

درواقع Worker یعنی یک سرویس جدا که کارهای سنگین یا زمان‌بر را در پس‌زمینه انجام می‌دهد.

در دات‌نت معمولاً:

- BackgroundService
- Windows Service
- Docker Container
- Hosted Service

است.

یعنی یک برنامه همیشه روشن.

جداست.

اگر download زیاد شد:

```text
Worker بیشتر
```

اضافه می‌کنی.

-----------------------------------

#### بخش Redis


فقط cache است.

-----------------------------------

#### بخش Storage


مجزاست.

-----------------------------------

### اگر ۱۰ میلیون صفحه ذخیره شود چه؟

مشکلی نیست چون:

- فایل‌ها در Object Storage هستند
- و DB فقط metadata دارد

این همان معماری سرویس‌های بزرگ است.

-----------------------------------

### نمونه Entity



```csharp
public class CachedPage
{
    public long Id { get; set; }

    public string Url { get; set; }

    public string UrlHash { get; set; }

    public string ShortCode { get; set; }

    public string StoragePath { get; set; }

    public DateTime CreatedAt { get; set; }
}
```

-----------------------------------

### نمونه Worker


```csharp
var html = await httpClient.GetStringAsync(url);

html = HtmlCleaner.RemoveScripts(html);

await storage.SaveAsync(path, html);
```

-----------------------------------

### چرا RemoveScripts مهم است؟


چون:
- امنیت
- XSS
- JS مخرب
- crypto miner

ممکن است داخل HTML باشد.

پس بهتر است:
- script حذف شود
- iframe محدود شود

-----------------------------------

### اگر سایت React بود؟


فقط HTML خام کافی نیست.

باید:
- Playwright
- Headless Browser

استفاده کنی.

ولی فقط اگر لازم شد.

-----------------------------------

#### نسخه MVP پیشنهادی 


اگر بخواهم واقعی طراحی کنی :

#### Stack

- ASP.NET Core
- RabbitMQ
- Redis
- PostgreSQL
- MinIO

-----------------------------------

### چرا؟


ASP.NET:
- سریع
- async عالی
- scale خوب

RabbitMQ:
- queue ساده

Redis:
- cache سریع

PostgreSQL:
- metadata

MinIO:
- فایل HTML

-----------------------------------

### نتیجه


الان سیستم عملاً تبدیل شد به:

```text
Distributed Page Cache System
```

نه Summary System.

و این خیلی:
- ساده‌تر
- سریع‌تر
- ارزان‌تر
- و scalable‌تر

است.