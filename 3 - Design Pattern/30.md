الگوی Singleton تضمین می‌کند که:

- فقط **یک instance** از یک کلاس وجود داشته باشد
- همه قسمت‌های برنامه به همان instance دسترسی داشته باشند

### موارد استفاده

- Logger
- Configuration
- Cache
- Connection manager

### مثال #C
```csharp

public sealed class Logger
{
    private static readonly Logger _instance = new Logger();

    private Logger() { }

    public static Logger Instance => _instance;

    public void Log(string message)
    {
        Console.WriteLine(message);
    }
}

```
استفاده:
```markdown

Logger.Instance.Log("Application started");

```
### مثال دنیای واقعی

تنظیمات سیستم عامل (System Configuration) فقط یک نمونه دارد.