بهترین معماری:

- داشتن Hub کوچک (Thin Hub)
- انتقال business logic به service ها
- استفاده از DI
- ذخیره connection‌ها در cache (Redis)
- استفاده از Event-driven architecture

مثال:

یک Hub فقط اتصال را مدیریت می‌کند، نه منطق سیستم.