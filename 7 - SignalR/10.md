هر اتصال در SignalR یک **شناسه یکتا** دارد.

```markdown

ConnectionId

```
کاربرد:

- ارسال پیام به یک کاربر خاص
- مدیریت اتصال‌ها

مثال:
```csharp

await Clients.Client(connectionId).SendAsync("ReceiveMessage", message);

```
