چون:

- ارتباط شبکه‌ای است
- عملیات I/O دارد
- باید non-blocking باشد

اگر async نباشد:

- در این حالت thread ها بلاک می‌شوند
- و performance کاهش پیدا می‌کند

مثال:
```csharp

public async Task SendMessage(string message)
{
    await Clients.All.SendAsync("ReceiveMessage", message);
}

```
